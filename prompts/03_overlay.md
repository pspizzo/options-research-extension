Instead of changing the background color of the strike price div, do this instead:

- Add a new class to the strike price cell, named "spizzo-strike"
- Add a new div inside the strike price element, after the current content: `<div class="spizzo-overlay-put">RATE</div>`

